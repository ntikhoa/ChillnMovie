<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ProgressBar
        android:id="@+id/progressBarScore"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:indeterminateOnly="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:progress="80"
        tools:progressDrawable="@drawable/circle_green" />

    <TextView
        android:id="@+id/textViewScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        app:layout_constraintBottom_toBottomOf="@+id/progressBarScore"
        app:layout_constraintEnd_toEndOf="@+id/progressBarScore"
        app:layout_constraintStart_toStartOf="@+id/progressBarScore"
        app:layout_constraintTop_toTopOf="@+id/progressBarScore"
        tools:text="80" />

</androidx.constraintlayout.widget.ConstraintLayout>